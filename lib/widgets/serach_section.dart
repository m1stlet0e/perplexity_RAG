import 'package:flutter/material.dart';

class SerachSection extends StatelessWidget {
  const SerachSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}